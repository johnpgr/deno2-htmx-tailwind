import {
    createIcons,
    Loader2,
    LockKeyhole,
    Mail,
    User,
} from "../node_modules/lucide/dist/esm/lucide.js"

createIcons({
    icons: {
        User,
        Mail,
        LockKeyhole,
        Loader2,
    },
})
